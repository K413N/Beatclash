import styled from "styled-components";
import { Wrapper } from "./GlobalStyles";

const Boards = () => {
    return(
        <Wrapper>
        <div>Boards</div>
        </Wrapper>
    )
}

export default Boards;