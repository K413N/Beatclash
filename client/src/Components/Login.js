import styled from "styled-components";
import { Wrapper } from "./GlobalStyles";

const Login = () => {
    return(
        <Wrapper>
        <div>Login</div>
        </Wrapper>
    )
}

export default Login;